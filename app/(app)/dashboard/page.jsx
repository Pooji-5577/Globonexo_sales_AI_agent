export const metadata = {
  title: "Dashboard — Globonexo Sales AI",
};

export default function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>TODO: migrate Dashboard from GlobonexoPrototype.jsx and wire to /api/dashboard</p>
    </div>
  );
}
