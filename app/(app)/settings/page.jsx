export const metadata = {
  title: "Settings — Globonexo Sales AI",
};

export default function SettingsPage() {
  return (
    <div>
      <h1>Settings</h1>
      <p>TODO: migrate Settings from GlobonexoPrototype.jsx and wire to /api/settings</p>
    </div>
  );
}
