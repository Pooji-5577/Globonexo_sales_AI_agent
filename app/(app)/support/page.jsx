export const metadata = {
  title: "Support — Globonexo Sales AI",
};

export default function SupportPage() {
  return (
    <div>
      <h1>Support</h1>
      <p>TODO: build support chat UI, wire to /api/support/tickets</p>
    </div>
  );
}
