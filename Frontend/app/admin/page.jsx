export const metadata = {
  title: "Admin Dashboard — Globonexo Sales AI",
};

export default function AdminPage() {
  return (
    <div>
      <h1>Admin Dashboard</h1>
      <p>TODO: build admin panel, wire to /api/admin/*</p>
    </div>
  );
}
